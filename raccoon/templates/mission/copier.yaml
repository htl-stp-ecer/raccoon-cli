_jinja_extensions:
  - jinja2_time.TimeExtension

_templates_suffix: .jinja

generated_at:
  when: false
  default: "{{ '%Y-%m-%d %H:%M:%S' | strftime }}"

mission_snake_case:
  type: str
mission_pascal_case:
  type: str